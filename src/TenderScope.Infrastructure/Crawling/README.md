# Crawling safety defaults

TenderScope adapters must respect robots directives and source terms, use the shared HTTP client, enforce bounded responses, back off on transient failures and never attempt to bypass CAPTCHA or access controls.
